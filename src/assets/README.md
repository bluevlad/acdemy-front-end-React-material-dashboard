# Assets Directory

This directory stores static assets and theming configurations for the application.

## Structure

- **images**: Static images used in the application (logos, backgrounds, etc.).
- **theme**: The base theme configuration (typography, colors, breakpoints, etc.) for Material UI.
- **theme-dark**: Theme configuration for Dark Mode.
