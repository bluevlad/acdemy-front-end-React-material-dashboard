# Utils Directory

This directory contains utility functions and helper modules.

## Files

- **commonUtils.js**: General purpose utility functions.
- **request.js**: HTTP request abstraction (likely wrapper around `superagent` or `axios`).
- **storage.js**: Helper functions for interacting with `localStorage` or `sessionStorage`.
