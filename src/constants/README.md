# Constants Directory

This directory holds constant values used throughout the application to avoid hardcoding strings and magic numbers.

## Files

- **index.js**: General application constants (e.g., API base URLs, fixed configuration values).
- **documentStatus.js**: Constants related to document or entity statuses (e.g., "Draft", "Published").
